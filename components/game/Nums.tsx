export default function Nums({ number }: { number: number }) {
    return (
        <div className="bg-white flex basis-[12.5%] items-center justify-center font-bold text-2xl">
            {number}
        </div>
    );
}
